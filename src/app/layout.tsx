import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Buscador de Preços - Compare e Economize",
  description: "Compare preços de produtos em múltiplas lojas online. Encontre as melhores ofertas na Amazon, Magazine Luiza e Casas Bahia.",
  keywords: "buscador, preços, comparação, ofertas, Amazon, Magazine Luiza, Casas Bahia",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
